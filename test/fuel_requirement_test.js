import { assertEquals } from "jsr:@std/assert";
import { fuelRequires } from "../src/fuel_requrierment.js";

Deno.test("small module", () => {
  assertEquals(fuelRequires("123"), 39);
});

Deno.test("small module", () => {
  assertEquals(fuelRequires("123"), 39);
});

Deno.test("small module", () => {
  assertEquals(fuelRequires("123"), 39);
});
